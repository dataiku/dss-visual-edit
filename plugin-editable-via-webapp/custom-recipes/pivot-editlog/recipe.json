{
    "meta": {
        "label": "Pivot editlog",
        "description": "Create a dataset whose rows correspond to unique keys from the editlog, columns are the original dataset's editable columns, and values are the last edits",
        "icon": "icon-repeat"
    },
    "kind": "PYTHON",
    "inputRoles": [
        {
            "name": "editlog",
            "label": "Edit log",
            "description": "Managed by webapp",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],
    "outputRoles": [
        {
            "name": "editlog_pivoted",
            "label": "Pivoted edit log",
            "description": "",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
        }
    ],
    "params": [],
    "resourceKeys": []
}
